STACK OPERATIONS :

AIM: To implement and show the stack operations in code

CODE:
#include<stdio.h>
void display(int stack[],int top){
   printf("stack elements are \n");
   for(int i=0;i<=top; i++){
	printf(">>%d",stack[i]);
   }
   printf("[top]-\n");
}
int main(){
    int stack_size,top=-1,choice=0,item;
    printf("enter the size of stack");
    scanf("%d",&stack_size);
    int stack[stack_size];
    while(choice!=3){
	printf("\n 1.push \n2. pop \n3. exit\n choose your option");
	scanf("%d",&choice);
	switch(choice)
	{
	case 1:
	if(top==stack_size-1){
		printf("stack overflow");
	   }
	else{
		printf("enter the value to push");
		scanf("%d",&item);
		top=top+1;
		stack[top]=item;
	   }	
	display(stack,top);
	break;
	case 2:
	if(top==-1){
		printf("stack underflow");
	   }	
	else{

		item=stack[top];
		top=top-1;
		printf("popped element is %d",item);
	    }
	
	display(stack,top);
	break;
	case 3:
	return 0;
	default:
	printf("incorrect choice");
	break;
	}
    }
